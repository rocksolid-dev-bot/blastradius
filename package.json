{
  "name": "blastradius",
  "version": "0.2.0",
  "description": "Rank outdated npm/pnpm/yarn dependencies by blast radius, not alphabet.",
  "license": "MIT",
  "type": "module",
  "private": false,
  "bin": {
    "blastradius": "dist/cli.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "typescript": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "vitest": "^2.0.0"
  }
}
